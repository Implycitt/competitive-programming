#include <bits/stdc++.h>

using namespace std;

// this file is not a codeforces problem; it serves only as a playground to test stuff.

int main() {
  if (string("aa") < string("bz")) cout << "YES";
}
